-r base.txt

pytest>=8.3
pytest-django>=4.9
pytest-cov>=6.0
factory-boy>=3.3
freezegun>=1.5
responses>=0.25
ruff>=0.9
django-debug-toolbar>=5.0

# Windows only. Lets Celery's filesystem broker transport run a real worker
# without Redis, which is useful for local development on machines where
# installing Redis is awkward. Production always uses Redis.
pywin32>=306; sys_platform == "win32"
